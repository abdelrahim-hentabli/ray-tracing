name: pre-commit

on:
  pull_request:

jobs:
  Build-And-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'true'
      - uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: libavutil59 libavcodec61 libavformat61 libswscale8 libswresample5
          version: 1.1
          execute_install_scripts: true
      - name: CMake config & build
        run:  |
          cmake -B build .
          cmake --build build --parallel 4
